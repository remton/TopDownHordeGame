using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PoolNoodle : MeleeWeapon
{
    /* TO DO (after MeleeWeapon is implemented properly):
     * Add pool noodle sprite/animation
     * Add water splash effect (press, hold, and release mouse to release a wave of water that knocks back and damages enemies)
     * Add different colors (maybe)
     * Add golden pool noodle (very rare, does big damage)
     */

    /*
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
